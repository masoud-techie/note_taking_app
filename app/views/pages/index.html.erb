<div class="d-flex flex-column justify-content-center align-items-center text-center" style="min-height: 70vh;">
  <h1 class="display-4 mb-4">Welcome to NotesApp</h1>
  <p class="lead mb-4">Take notes, mark favorites, and keep your ideas organized — all in one place!</p>

  <% if user_signed_in? %>
    <%= link_to "Go to My Notes", notes_path, class: "btn btn-primary btn-lg me-2" %>
  <% else %>
    <%= link_to "Sign Up", new_user_registration_path, class: "btn btn-success btn-lg me-2" %>
  <% end %>
</div>
